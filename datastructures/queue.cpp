#include <bits/stdc++.h>

struct Node{
    // value of node
    int val;
    // pointer to next node
    Node * next = NULL;
};

int main(){
    int n;
    std::vector<int> A;
    std::cin >> n;

    srand(time(NULL));
    for(int i=0; i<n; i++){
        
    }

    std::cout << "The starting array is the following : \n";



    return 0;
}